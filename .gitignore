node_modules/
.next/
.turbo/
.vercel/
out/
dist/
.env
.env.local
.env.*.local
!.env.example
*.log
.DS_Store
coverage/
.idea/
.vscode/
!.vscode/extensions.json
src/app/(payload)/admin/importMap.js
src/payload-types.ts
.claude/settings.local.json
.neon
next-env.d.ts
tsconfig.tsbuildinfo
# Scratch notes live here untracked; two documents the rebuild depends on are
# checked in: the pre-teardown content export (plan TASK-001) and the ledger of
# her supplied source copy, which is what her sign-off is given against.
# `docs/*` rather than `docs/`: git does not descend into an ignored directory,
# so a whole-directory pattern makes the negations below unreachable.
docs/*
!docs/content-export-2026-08.md
!docs/source-copy-2026-08-07.md

# AI-generated prelude source PNGs. Staged into references/cosmos-sources/prelude/
# for the bake (that dir has its own .gitignore blocking PNGs). Only the baked
# WebPs in public/art/cosmos/prelude/ are checked in.
generated/
.vercel
.impeccable
