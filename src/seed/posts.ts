import type { Payload } from "payload";

export async function seedPosts(payload: Payload, opts: { force?: boolean }) {
  // Example:
  // await payload.create({ collection: 'posts', data: { title: 'Hello', slug: 'hello', ... } })
  void payload;
  void opts;
}
