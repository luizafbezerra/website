import type { Payload } from "payload";

export async function seedSettings(payload: Payload, opts: { force?: boolean }) {
  // Example:
  // await payload.updateGlobal({ slug: 'settings', data: { siteName: 'My Site', description: '...' } })
  void payload;
  void opts;
}
