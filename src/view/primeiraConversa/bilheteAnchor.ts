/**
 * The in-page id of the bilhete section, shared by the section that owns it and
 * by the abertura's jump link — so the two can never disagree about the address.
 */
export const BILHETE_ANCHOR = "bilhete";
